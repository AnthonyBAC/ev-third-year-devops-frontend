# Copia este archivo a .env y completa los valores para correr el stack local:
#   cp .env.example .env && docker compose up -d --build
#
# En el deploy a EC2 estos valores vienen de GitHub Secrets, no de este archivo.

# Usuario de Docker Hub (prefijo de la imagen front-despacho)
DOCKER_USERNAME=tu_usuario_dockerhub

# IP privada del EC2 backend (a donde nginx hace proxy de /api/v1/*)
# En local puedes apuntar a la IP/host donde corra el backend.
BACKEND_PRIVATE_IP=172.31.0.0
